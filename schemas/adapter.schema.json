{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/zizzfizzix/agentic-engineering-framework/schemas/adapter.schema.json",
  "title": "adapter.json",
  "description": "An adapter declares which skills it augments and the slot content it provides. The renderer fills matching slots in generic skill bodies and prunes the rest.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "Adapter id, e.g. 'drizzle'."
    },
    "axis": {
      "type": "string",
      "description": "Variation family: orm | ui | stack | harness."
    },
    "description": {
      "type": "string"
    },
    "skillsDir": {
      "type": "string",
      "description": "Harness only: directory the per-skill links go in."
    },
    "linkBase": {
      "type": "string",
      "description": "Harness only: relative link target base from skillsDir."
    },
    "augments": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Skill ids this adapter contributes slot content to."
    },
    "slots": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "propertyNames": {
        "pattern": "^[a-z][\\w-]*\\.[a-z][\\w-]*$"
      },
      "description": "slotName -> fragment path, relative to the adapter dir."
    },
    "tiers": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "skill": {
            "type": "string"
          },
          "tier": {
            "type": "string"
          }
        },
        "required": [
          "skill",
          "tier"
        ],
        "additionalProperties": false
      },
      "description": "Optional skills this adapter adds, and the tier each joins."
    }
  },
  "required": [
    "name",
    "axis"
  ],
  "additionalProperties": false
}
