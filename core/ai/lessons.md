# Lessons Learned

Record patterns, mistakes, and insights discovered during development. Review at session start.
AI agents append to this file as they work; each entry should be a short, durable rule that
prevents a recurring mistake.

> Scope tags (used by the `improve-framework` feedback loop):
>
> - `project` (default) — stays in this file, local to this repo forever.
> - `framework` / `adapter:<axis>:<name>` — generic insight; captured to
>   `.ai/framework-feedback/` for upstreaming.

<!-- Add lessons below this line. -->
